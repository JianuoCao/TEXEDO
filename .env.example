# Copy to .env and adjust, or export these in your shell.
# Defaults (used when unset): ASSETS -> <repo>/assets, DATA -> <repo>/data

# Where downloaded checkpoints live.
TSD_ASSETS=./assets

# Where datasets live (TEXEDO download + prepared training layout).
TSD_DATA=./data
